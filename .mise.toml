# Mise configuration for EchoVault
# https://mise.jdx.dev/
#
# Setup: mise run setup

[tools]
node = "22"
rust = "latest"
pnpm = "latest"
uv = "latest"

[settings]
trusted_config_paths = ["."]

[tasks.setup]
description = "Setup development environment"
run = """
#!/bin/bash
set -e

echo "🔧 Installing mise tools (node, rust, pnpm, uv)..."
mise install

echo "📦 Node dependencies..."
pnpm install

echo "🦀 Rust build..."
cargo build

echo "⬇️ Download rclone..."
node scripts/download-rclone.mjs

echo "🪝 Pre-commit hooks..."
uv run pre-commit install

echo ""
echo "✅ Setup complete!"
echo "   → Dev: pnpm dev"
echo "   → Tauri: cargo tauri dev"
"""
